apiVersion: core.oam.dev/v1alpha2
kind: TraitDefinition
metadata:
  name: topologySpreadConstraints.trait.abm.io
spec:
  runtime: pre
  appliesToWorkloads:
    - "*"
  definitionRef:
    name: topologySpreadConstraints.schema.abm.io
